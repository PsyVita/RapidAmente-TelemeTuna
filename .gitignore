# Files to ignore when pushing to git repository.

# Secrets
.env
.env.*
!.env.example

# Node libraries — rebuildable with `npm install` from package.json
node_modules/

# Editor / OS junk
.vscode/
.DS_Store